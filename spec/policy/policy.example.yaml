# Example only; normative field validation is in policy.schema.json.
version: 1

trusted_signers:
  - alice.dev
  - opertus.systems

capability_ceiling:
  fs:
    read:
      - /data
    write:
      - /tmp
  net:
    - https://api.open-meteo.com
    - https://api.github.com
  env:
    - HOME
    - PATH
  exec: false
  time: false
